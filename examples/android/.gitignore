# SDK の場所はホスト環境（ANDROID_HOME）から取る。local.properties はユーザー固有の
# 絶対パスが入るのでコミットしない（ADR 0006 決定 2 と同じ理由）。
local.properties

# scripts/build_android.sh の生成物。バイナリと生成コードは git 履歴に積まない
# （ADR 0004 決定 8 の方針を Android 側でも踏襲）。
app/src/main/jniLibs/
app/src/generated/

.gradle/
build/
app/build/
