# SDK の場所はホスト環境（ANDROID_HOME）から取る。local.properties はユーザー固有の
# 絶対パスが入るのでコミットしない（ADR 0006 決定 2 と同じ理由）。
local.properties

# 配布された .aar（GitHub Release から DL、または scripts/build_aar.sh の出力をコピー）。
# バイナリは git 履歴に積まない（ADR 0004 決定 8 の方針を Android 側でも踏襲）。
app/libs/

.gradle/
build/
app/build/
